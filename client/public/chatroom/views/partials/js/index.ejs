<script>
    const username = document.querySelector("input[type=text]");
    const joinbtn = document.querySelector('#join-btn>a')
    const submitbtn = document.querySelector('.submitbtn')
    window.onload = (e) => {
      username.focus();
    };

    joinbtn.onclick = e => {
      e.preventDefault()
      username.focus()
    }

    // set min & max length of user's display name
    let {max,min} = {max:12,min:4}
    console.log(max,min)
    username.setAttribute('maxlength',max)
    username.setAttribute('minlength',min)


    // submitbtn.onclick = async e => {
    //   e.preventDefault()
    //   const contentObj = {
    //     header:{'Content-Type':'application/json'},
    //     method:'post',
    //     body:JSON.stringify({username: username.value})
    //   }
    //   await fetch('/module/chatroom/login-attempt',{
    //     method:"POST",
    //     body:JSON.stringify({username: username.value})})
    // }
  </script>